# Hospital Management System - Offline Software
# Python 3.7+ required

# This project uses only built-in Python libraries:
tkinter == 1.0.0
# - sqlite3 (database)
# - datetime, uuid, os, sys, typing (standard library)
#
# No external dependencies are required to run this application.

# Optional dependencies (uncomment if needed):
# python-dateutil==2.8.2  # For enhanced date handling
# reportlab==4.0.4        # For PDF generation (reports/printouts)

# Build dependencies:
pyinstaller>=5.0.0        # For creating .exe files
reportlab>=4.0.0          # For PDF generation (reports/printouts)
Pillow>=10.0.0            # For image handling (logo display)

# Flask API dependencies (for web mode):
flask>=2.0.0              # Flask web framework
flask-cors>=3.0.0         # CORS support for Flask API